git本地操作：
git --help
	调出git帮助文档
git 命令 --help
	查看某个指令的帮助文档
git --verison
	查看终端git版本
git init
	生成空的本地仓库
git add 文件
	将文件添加到暂存区
git config --global user.email "邮箱"
git config --global user.name "名字"
	初次commit之前，需要配置邮箱及用户名
git commit -m "文件"
	将暂存区的文件提交到本地仓库


git远程操作：
